# PSG Hospital Navigation Frontend

A React + Vite frontend for the PSG Hospital Navigation system.

## Setup

1. Install dependencies:
   ```bash
   cd frontend
   npm install
   ```
2. Run the app:
   ```bash
   npm run dev
   ```

The development server is configured to proxy `/api` requests to `http://localhost:5000`.
